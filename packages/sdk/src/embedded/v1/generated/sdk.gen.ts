// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { ActivateIntegrationData, ActivateIntegrationErrors, ActivateIntegrationResponses, CreateEndUserConnectionData, CreateEndUserConnectionErrors, CreateEndUserConnectionResponses, CreateEndUserData, CreateEndUserErrors, CreateEndUserResponses, CreateHubConnectionTemplateData, CreateHubConnectionTemplateErrors, CreateHubConnectionTemplateResponses, CreateHubData, CreateHubErrors, CreateHubResponses, CreateIntegrationData, CreateIntegrationErrors, CreateIntegrationResponses, DeleteActivationData, DeleteActivationErrors, DeleteActivationResponses, DeleteIntegrationData, DeleteIntegrationErrors, DeleteIntegrationResponses, GetActivationData, GetActivationErrors, GetActivationResponses, GetEmbeddedIntegrationData, GetEmbeddedIntegrationErrors, GetEmbeddedIntegrationResponses, GetEndUserConnectionData, GetEndUserConnectionErrors, GetEndUserConnectionResponses, GetEndUserData, GetEndUserErrors, GetEndUserResponses, GetHubData, GetHubErrors, GetHubResponses, HealthCheckData, HealthCheckResponses, InitialiseOAuth2EmbeddedConnectionData, InitialiseOAuth2EmbeddedConnectionErrors, InitialiseOAuth2EmbeddedConnectionResponses, ListEndUserActivationsData, ListEndUserActivationsErrors, ListEndUserActivationsResponses, ListEndUserConnectionsData, ListEndUserConnectionsErrors, ListEndUserConnectionsResponses, ListEndUserIntegrationsData, ListEndUserIntegrationsErrors, ListEndUserIntegrationsResponses, ListEndUsersData, ListEndUsersErrors, ListEndUsersResponses, ListHubsData, ListHubsErrors, ListHubsResponses, ListIntegrationConnectionTemplatesData, ListIntegrationConnectionTemplatesErrors, ListIntegrationConnectionTemplatesResponses, ListIntegrationsData, ListIntegrationsErrors, ListIntegrationsResponses, UpdateIntegrationMetadataData, UpdateIntegrationMetadataErrors, UpdateIntegrationMetadataResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * List Hubs
 *
 * ListHubs returns all the hubs for the given organisation, but is returned as a paginated list.
 *
 */
export const listHubs = <ThrowOnError extends boolean = false>(options: Options<ListHubsData, ThrowOnError>) => (options.client ?? client).get<ListHubsResponses, ListHubsErrors, ThrowOnError>({ url: '/o/{organisation_id}/hubs', ...options });

/**
 * Create a new Hub
 *
 * Create a new Hub
 */
export const createHub = <ThrowOnError extends boolean = false>(options?: Options<CreateHubData, ThrowOnError>) => (options?.client ?? client).post<CreateHubResponses, CreateHubErrors, ThrowOnError>({
    url: '/hubs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Retrieve a Hub
 *
 * GetHub returns the Hub for the given ID.
 *
 */
export const getHub = <ThrowOnError extends boolean = false>(options: Options<GetHubData, ThrowOnError>) => (options.client ?? client).get<GetHubResponses, GetHubErrors, ThrowOnError>({ url: '/hubs/{hub_id}', ...options });

/**
 * List End Users
 *
 * ListEndUsers returns a paginated list of all the End Users for the given Hub.
 *
 */
export const listEndUsers = <ThrowOnError extends boolean = false>(options: Options<ListEndUsersData, ThrowOnError>) => (options.client ?? client).get<ListEndUsersResponses, ListEndUsersErrors, ThrowOnError>({ url: '/hubs/{hub_id}/users', ...options });

/**
 * Create an End User
 *
 * CreateEndUser creates a new End User for the given Hub.
 *
 * The end user requires a unique `externalId` which is recommended to be the same as the user's ID on the
 * Primary Connector for the Hub. This is an immutable field so should not be set to something that may change
 * such as an email address.
 *
 * Before an end user can activate an Integration, they must also have a Connection defined for the Primary
 * Connector. This may be created as part of this request, a separate request to CreateConnection, or as part of
 * the request in the first Activate call.
 *
 */
export const createEndUser = <ThrowOnError extends boolean = false>(options: Options<CreateEndUserData, ThrowOnError>) => (options.client ?? client).post<CreateEndUserResponses, CreateEndUserErrors, ThrowOnError>({
    url: '/hubs/{hub_id}/users',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Retrieve an End User
 *
 * GetEndUser returns the End User for the given ID.
 *
 */
export const getEndUser = <ThrowOnError extends boolean = false>(options: Options<GetEndUserData, ThrowOnError>) => (options.client ?? client).get<GetEndUserResponses, GetEndUserErrors, ThrowOnError>({ url: '/hubs/{hub_id}/users/{user_id}', ...options });

/**
 * List End User Connections
 *
 * ListEndUserConnections returns all the Connections for the given End User.
 *
 * The result may be filtered by `hub_connector_id` to only return connections for a specific Hub Connector.
 *
 */
export const listEndUserConnections = <ThrowOnError extends boolean = false>(options: Options<ListEndUserConnectionsData, ThrowOnError>) => (options.client ?? client).get<ListEndUserConnectionsResponses, ListEndUserConnectionsErrors, ThrowOnError>({
    querySerializer: { parameters: { hub_connector_id: { array: { explode: false } } } },
    url: '/hubs/{hub_id}/users/{user_id}/connections',
    ...options
});

/**
 * Create End User Connection
 *
 * CreateEndUserConnection creates a new Connection for the given User. The Connection is used to authenticate the
 * user to a Hub Connector.
 *
 * This endpoint is typically only used for more bespoke implementations where an End User needs to authenticate
 * to a Connector outside of the normal activation flow, for example where credentials are provided by another API
 * and not through End User actions in the embedded UI.
 *
 */
export const createEndUserConnection = <ThrowOnError extends boolean = false>(options: Options<CreateEndUserConnectionData, ThrowOnError>) => (options.client ?? client).post<CreateEndUserConnectionResponses, CreateEndUserConnectionErrors, ThrowOnError>({
    url: '/hubs/{hub_id}/users/{user_id}/connections',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Integrations
 *
 * ListIntegrations returns all the integrations for the given Hub.
 *
 * This API should not be exposed to End Users via the embedded UI since it includes integrations which have not
 * been published.
 *
 */
export const listIntegrations = <ThrowOnError extends boolean = false>(options: Options<ListIntegrationsData, ThrowOnError>) => (options.client ?? client).get<ListIntegrationsResponses, ListIntegrationsErrors, ThrowOnError>({ url: '/hubs/{hub_id}/integrations', ...options });

/**
 * Create Integration
 *
 * Create Integration
 */
export const createIntegration = <ThrowOnError extends boolean = false>(options: Options<CreateIntegrationData, ThrowOnError>) => (options.client ?? client).post<CreateIntegrationResponses, CreateIntegrationErrors, ThrowOnError>({
    url: '/hubs/{hub_id}/integrations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an integration.
 *
 * DeleteIntegration deletes the integration.
 *
 * If the integration is deployed it will return an error.
 *
 */
export const deleteIntegration = <ThrowOnError extends boolean = false>(options: Options<DeleteIntegrationData, ThrowOnError>) => (options.client ?? client).delete<DeleteIntegrationResponses, DeleteIntegrationErrors, ThrowOnError>({ url: '/integrations/{integration_id}', ...options });

/**
 * List Hub Connectors
 *
 * ListIntegrationConnectors returns all the Hub Connectors for the given Integration.
 *
 * This API should not be exposed to End Users via the embedded UI since it includes connectors which have not
 * been published.
 *
 */
export const listIntegrationConnectionTemplates = <ThrowOnError extends boolean = false>(options: Options<ListIntegrationConnectionTemplatesData, ThrowOnError>) => (options.client ?? client).get<ListIntegrationConnectionTemplatesResponses, ListIntegrationConnectionTemplatesErrors, ThrowOnError>({ url: '/integrations/{integration_id}/connection-templates', ...options });

/**
 * Create a Hub Connector
 *
 * CreateHubConnectionTemplate creates a new Hub Connector for the given Integration.
 *
 * A Hub Connector is a lightweight wrapper around a Connector from Connect API, but includes additional
 * information to make embedding the integration simpler:
 *
 * - A name/reference for the Hub Connector, which represents the purpose of the Connector in the context of the
 * Hub's Integration. For example, where an Integration is synchronising data between two accounts of the same
 * system, each Hub Connector may be referenced, "Source" and "Target", even though they are both referencing the
 * same underlying Connector.
 * - Which `AuthSchemeConfig` to use, so that when the end user activates the integration, they
 * are prompted to authenticate in the correct way.
 *
 */
export const createHubConnectionTemplate = <ThrowOnError extends boolean = false>(options: Options<CreateHubConnectionTemplateData, ThrowOnError>) => (options.client ?? client).post<CreateHubConnectionTemplateResponses, CreateHubConnectionTemplateErrors, ThrowOnError>({
    url: '/integrations/{integration_id}/connection-templates',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Internal endpoint used to update metadata for an integration.
 *
 * UpdateIntegrationMetadata is used to update which hub connectors are in use by the integration.
 * This will only be called from switchboard-api.
 *
 */
export const updateIntegrationMetadata = <ThrowOnError extends boolean = false>(options: Options<UpdateIntegrationMetadataData, ThrowOnError>) => (options.client ?? client).put<UpdateIntegrationMetadataResponses, UpdateIntegrationMetadataErrors, ThrowOnError>({
    url: '/integrations/{integration_id}/metadata',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Retrieve an End User Connection, used in sb-actions to get credentials for an end user.
 *
 * GetEndUserConnection returns the Connection for the given ID.
 *
 */
export const getEndUserConnection = <ThrowOnError extends boolean = false>(options: Options<GetEndUserConnectionData, ThrowOnError>) => (options.client ?? client).get<GetEndUserConnectionResponses, GetEndUserConnectionErrors, ThrowOnError>({ url: '/connection-templates/{connection_template_id}/users/{user_id}', ...options });

/**
 * List End User Integrations
 *
 * ListEndUserIntegrations returns all integrations which are available to the given End User.
 *
 * This API should be used to populate the embedded UI with the integrations that the end user can activate.
 *
 * If the `user_id` is referencing the external ID of the user, then the `hub` query parameter is required in order
 * to identify the correct Hub to retrieve the integrations from.
 *
 */
export const listEndUserIntegrations = <ThrowOnError extends boolean = false>(options: Options<ListEndUserIntegrationsData, ThrowOnError>) => (options.client ?? client).get<ListEndUserIntegrationsResponses, ListEndUserIntegrationsErrors, ThrowOnError>({ url: '/embed/{hub_id}/users/{user_id}/integrations', ...options });

/**
 * Retrieve an Embedded Integration
 *
 * GetEmbeddedIntegration returns the Integration for the given ID to be displayed to the user in the embedded UI.
 *
 */
export const getEmbeddedIntegration = <ThrowOnError extends boolean = false>(options: Options<GetEmbeddedIntegrationData, ThrowOnError>) => (options.client ?? client).get<GetEmbeddedIntegrationResponses, GetEmbeddedIntegrationErrors, ThrowOnError>({ url: '/embed/{hub_id}/users/{user_id}/integrations/{integration_id}', ...options });

/**
 * Initialise OAuth 2.0 Connection
 *
 * InitialiseOAuth2Connection returns a URL to redirect the user to an OAuth 2.0 Provider's Authorize endpoint.
 *
 * Only URLs constructed via this API can be used to connect to an OAuth 2.0 connector within
 * Versori, since a `state` parameter is generated and validated upon redirection to the
 * redirect_uri.
 *
 * Additional query parameters can be added to the resultant URL via the `additionalParams`
 * property in the body. In addition to the standard parameters defined in the body, this API
 * adds the following parameters:
 *
 * - `redirect_uri`: The URI to redirect the user to after the OAuth 2.0 Provider has
 * authenticated the user.
 * - `state`: A secure string generated by the API to authenticate requests to the redirect URI.
 * - `prompt`: See documentation on the `prompt` property of the request body.
 * - `scope`: See documentation on the `scopes` property of the request body.
 * - `response_type`: The response type to use. This is always `code` for OAuth 2.0.
 * - `client_id`: The client ID provided by the request body, this must match the client ID
 * which is stored under the provided `credentialId` otherwise the handling of the redirect
 * request will fail.
 *
 * Full details on the standard parameters can be found in the OIDC Core specification:
 * https://openid.net/specs/openid-connect-core-1_0.html#AuthRequest
 *
 */
export const initialiseOAuth2EmbeddedConnection = <ThrowOnError extends boolean = false>(options: Options<InitialiseOAuth2EmbeddedConnectionData, ThrowOnError>) => (options.client ?? client).post<InitialiseOAuth2EmbeddedConnectionResponses, InitialiseOAuth2EmbeddedConnectionErrors, ThrowOnError>({
    url: '/embed/{hub_id}/connector/{connector_id}/oauth2/initialise',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List End User Activations
 *
 * ListEndUserActivations returns all activations which are available to the given End User. This API can be
 * filtered to only return activations for a given integration.
 *
 */
export const listEndUserActivations = <ThrowOnError extends boolean = false>(options: Options<ListEndUserActivationsData, ThrowOnError>) => (options.client ?? client).get<ListEndUserActivationsResponses, ListEndUserActivationsErrors, ThrowOnError>({ url: '/embed/{hub_id}/users/{user_id}/activations', ...options });

/**
 * Activate Integration
 *
 * ActivateIntegration activates an Integration for an End User. This is the final step in the activation
 * process and will create the necessary connections and configurations to allow the Integration to run.
 *
 * This endpoint can either reference existing connections or create new ones. Once these connections are
 * resolved/created, if there are any missing connections to what the integration requires, an error will be
 * returned.
 *
 */
export const activateIntegration = <ThrowOnError extends boolean = false>(options?: Options<ActivateIntegrationData, ThrowOnError>) => (options?.client ?? client).post<ActivateIntegrationResponses, ActivateIntegrationErrors, ThrowOnError>({
    url: '/activations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Delete an Activation
 *
 * DeleteActivation deletes the Activation for the given ID.
 *
 */
export const deleteActivation = <ThrowOnError extends boolean = false>(options: Options<DeleteActivationData, ThrowOnError>) => (options.client ?? client).delete<DeleteActivationResponses, DeleteActivationErrors, ThrowOnError>({ url: '/activations/{activation_id}', ...options });

/**
 * Retrieve an Activation
 *
 * GetActivation returns the Activation for the given ID.
 *
 */
export const getActivation = <ThrowOnError extends boolean = false>(options: Options<GetActivationData, ThrowOnError>) => (options.client ?? client).get<GetActivationResponses, GetActivationErrors, ThrowOnError>({ url: '/activations/{activation_id}', ...options });

/**
 * Health Check
 *
 * HealthCheck returns a successful status if the API is healthy.
 *
 */
export const healthCheck = <ThrowOnError extends boolean = false>(options?: Options<HealthCheckData, ThrowOnError>) => (options?.client ?? client).get<HealthCheckResponses, unknown, ThrowOnError>({ url: '/healthz', ...options });
